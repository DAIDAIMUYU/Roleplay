# 19_阶段验收清单：阶段 0-10

> 每完成一个阶段，必须逐项打勾，更新进化记录，并提交 Git。未通过验收不得进入下一阶段。

## 阶段 0：项目清理、文档保护与干净底座

- [x] docs/roleplay-tavern/ 已存在且完整。
- [x] supabase/ 已存在且完整。
- [x] README.md 已存在。
- [x] .env.example 已存在。
- [x] 基础目录结构已建立。
- [x] 基础路由占位已建立。
- [x] 基础样式变量已建立。
- [x] AI 删除保护规则已写入文档。
- [x] 项目可启动。
- [x] Git commit：phase-0-foundation (355860f)。

## 阶段 1：运行模式 + Auth + 数据库 + RLS + Repository

- [x] Demo / User / Admin / Owner 模式定义已实现。
- [x] Supabase Auth 已接入。
- [x] profiles / user_roles 已创建（含 0005 bootstrap trigger + AuthProvider 兜底）。
- [x] 核心业务表 migrations 已建立（0001 含全部 17 表）。
- [x] RLS 已启用（SQL 策略已就绪）。
- [ ] 用户 A/B 私有数据隔离通过测试（需 Supabase 实例验证）。
- [x] Admin 默认不能读取普通用户 private 数据（RLS 策略已实现）。
- [x] Repository 层已建立。
- [x] 组件没有直接散落 Supabase 查询。
- [x] Git commit：phase-1-auth-db-repository (11d3498)。

## 阶段 2：产品信息架构 + UI App Shell

- [x] Hall 已完成 · Chat Room 壳已完成 · Studio 壳已完成 · Settings 壳已完成 · Admin 入口已预留。
- [x] 桌面端布局清晰（侧栏 256px + Chat 三栏）。
- [x] 移动端 App 布局可用（底部 4 Tab + 条件 Admin）。
- [x] Demo 状态、未登录状态、无 API 状态已设计。
- [x] 空状态、错误状态、加载状态已覆盖。
- [x] Onboarding 已有基础流程。
- [x] 参考图仅作为风格参考，不做强制复刻。
- [x] Git commit：phase-2-app-shell (4bf88f1)。

## 阶段 3：Provider Gateway + Demo Mock + BYOK 本地版

- [x] Provider Gateway 已建立（模式路由：Guest→Mock, Auth→DeepSeek/OpenAI）。
- [x] Mock Provider 已完成（标记 [Demo Mock]，零网络调用）。
- [x] Demo 模式只走 Mock Provider。
- [x] DeepSeek Provider 已完成（连接测试 + 非流式 + SSE 流式）。
- [x] OpenAI-compatible Provider 已完成（自定义 baseURL）。
- [x] 用户可输入 BYOK 临时 Key（session_only）· 可选择本设备保存（local_device）。
- [x] 阶段 3 不上传用户 API Key 到服务器。
- [x] 连接测试可用 · 401/402/429/网络失败有中文友好提示。
- [x] Git commit：phase-3-provider-byok-local (90a74b2)。

## 阶段 4：核心聊天闭环

- [x] 可创建会话 · 可保存消息 · 可读取历史消息。
- [x] 可真实调用用户 Provider（BYOK DeepSeek/OpenAI）。
- [x] 流式输出可用（Mock 逐字 + 真实 SSE）。
- [x] 停止生成（AbortController）· 重新生成 · 编辑用户消息后重发。
- [x] 删除/隐藏消息 · 复制消息 · 错误重试 · 错误翻译。
- [x] 自动保存状态（saveStatus: idle/saving/saved/error）。
- [x] 滚动到底部按钮（新消息浮标）· 最近打开会话恢复。
- [x] 移动端输入与键盘适配。
- [x] 编辑重发后旧消息写入 message_revisions（阶段 7 完成）。
- [ ] 【P2】消息删除仅为客户端移除，未同步 DB deleted_at。
- [x] Git commit：phase-4-core-chat (6069749)。

## 阶段 5：角色卡 / 提示词 / 会话 / 标签 / 搜索基础

- [x] 角色 CRUD 可用（11 字段 card_json）。
- [ ] 【P2】角色版本历史未实现（留到未来阶段）。
- [x] 角色收藏/归档/删除可用（软删除）。
- [ ] 【P2】角色复制·角色导入导出未实现（阶段 8 已实现全量导出导入，角色级复制未单独实现）。
- [x] 提示词模板 CRUD 可用（7 类分类 + 6 变量 + 点击插入按钮）。
- [x] 模板套用可用（右侧上下文控制台添加/移除/启用/禁用）。
- [x] 最终 Prompt 预览可用（ContextPreview "最终 System Prompt" 折叠面板）。
- [x] 会话 CRUD 可用 · 会话可绑定角色（primary_character_id + session_participants）。
- [x] 会话摘要字段可编辑（ContextPreview 内嵌编辑器）。
- [x] 角色/会话/模板标签可用 · 搜索可用。
- [x] 右侧上下文控制台支持动态添加/移除/启用/禁用（模板/世界书/记忆/摘要）。
- [x] ContextPanel.tsx 死代码文件已删除（阶段 7 完成）。
- [x] Git commit：phase-5-character-prompt-session (8188f0e)。

## 阶段 6：世界书 / 记忆 / 上下文引擎 / 快捷指令

- [x] 世界书 CRUD · 世界书条目 CRUD（含 keywords/priority/enabled/scope/category）。
- [x] 关键词触发（includes + lowercase 简单匹配）· 优先级和 scope 过滤。
- [x] 世界书命中解释（ContextPreview 折叠：命中条目绿色/跳过条目红色+原因）。
- [x] 记忆 CRUD（手动，7 类）。
- [x] 记忆注入预览（添加/启用/禁用/移除）· 手动选择，不自动注入全部。
- [x] Context Builder 统一构建（角色+模板+世界书+记忆+摘要 → System Prompt）。
- [x] Context Preview 折叠面板 · Context Debugger（命中/跳过/Token 明细）。
- [ ] 【P2】context_runs DB 持久化未完成（前端 Debugger 已实现，DB 写入留到阶段 7-8）。
- [x] Token 预算分项显示（各有限额，超预算跳过低优先级）。
- [x] 会话摘要已支持 AI 生成（Provider 压缩对话 → 预览 → 确认保存）。
- [ ] 【P2】快捷指令 /summary /memory /scene /ooc 未实现（阶段 9）。
- [x] Git commit：phase-6-worldbook-memory-context (a931cc7)。

## 阶段 7：消息版本历史 / 分支底座 / 软删除 / context_runs 持久化

- [x] 消息编辑后旧版本写入 message_revisions，UI 左右箭头切换查看。
- [x] 用户消息和 AI 回复均支持版本切换。
- [x] 编辑后不出现两条重复用户气泡。
- [x] 旧 assistant 回复软删除（deleted_at）并从主线隐藏。
- [x] branches 表补充字段 + 自动创建默认 branch + active_branch_id。
- [x] 全部业务表软删除（characters/sessions/messages/templates/worldbooks/entries/memories）。
- [x] context_runs 持久化，每次发送消息写入 DB，message_id 绑定 assistant 回复。
- [x] context_runs 不保存 API Key。
- [x] 删除 ContextPanel.tsx 死代码。
- [x] Git commit：phase-7-version-history-branches-soft-delete (e84b1a1)。

## 阶段 8：数据管理 / 导出导入 / 回收站

- [x] 设置中心已接入真实"数据管理"入口：`/settings/data`。
- [x] 本地 JSON 导出（角色/模板/世界书/记忆/会话/分支/消息/版本历史）。
- [x] 导出默认不包含 context_runs、API Key、localStorage、provider secrets。
- [x] 导入流程：先预览、后导入，默认"作为副本导入"（生成新 ID）。
- [x] 支持"跳过已存在项目"选项。
- [x] 回收站：基于 deleted_at 筛选，支持会话/消息/角色/模板/世界书/条目/记忆恢复。
- [x] 世界书恢复支持"恢复本体"和"恢复并恢复条目"。
- [x] 不新增 migration，复用 0007 软删除字段。
- [x] Git commit：phase-8-data-management-and-hosted-encrypted-api-keys (fe37435)。

## 阶段 8.1：托管加密 API Key / Edge Functions

- [x] 新增 `0008_phase8_1_hosted_encrypted_api_keys.sql`。
- [x] provider_credentials / provider_credential_secrets 双表隔离 + RLS。
- [x] provider_credential_secrets 不给普通前端 select 策略。
- [x] Settings 页面支持"托管加密 / 跨设备同步"。
- [x] 6 个 Edge Functions（save/list/test/set-default/delete/chat）。
- [x] AES-GCM 加密 + API_KEY_ENCRYPTION_SECRET。
- [x] 托管模式聊天通过 Edge Function 代理，前端不持有明文 Key。
- [x] 导出/导入/context_runs/backup 均不包含托管 Key 明文或密文。
- [x] Hosted 模式当前为非流式。
- [x] Git commit：phase-8-data-management-and-hosted-encrypted-api-keys (fe37435)。

## 阶段 9：长会话分页 / AI 记忆建议

- [x] 聊天页首屏只加载最近一页消息（50 条）。
- [x] "加载更早消息"入口 + 滚动位置保持。
- [x] Context Builder 独立从 DB 取最近有效消息（40 条），不依赖 UI 全量加载。
- [x] deleted / superseded / 非当前分支消息不进入上下文和分页。
- [x] AI 记忆建议：手动触发 → 预览 → 确认后写入 memories。
- [x] 会话级提炼（最近 20 条）和单条消息提炼。
- [x] memories.status = suggested / active / disabled / deleted（复用现有字段）。
- [x] suggested / disabled / deleted 记忆不注入 System Prompt。
- [x] 消息气泡新增"提炼记忆"按钮。
- [x] 右侧 Context Preview 新增候选记忆收件箱。
- [x] 不新增 migration。
- [x] Git commit：phase-9-pagination-memory-suggestions (d2e2a54)。

## 阶段 9.5：总体审计与产品收口检查

- [x] Git/构建/文档完整性检查通过。
- [x] 阶段 0-10 完成度总表已更新。
- [x] 核心产品链路审计完成（15 条链路全部可用）。
- [x] 数据库与 migration 审计完成（8 个 migration 对齐）。
- [x] 安全边界审计完成（无 P0 安全问题）。
- [x] 移动端体验审计完成（发现 hover 依赖 P1 问题）。
- [x] 性能与体积审计完成（建议 code splitting）。
- [x] 部署前准备清单已输出。
- [x] P0/P1/P2 遗留问题清单已输出。
- [x] 阶段 10 建议范围已明确。
- [x] 审计报告：`docs/roleplay-tavern/progress/阶段9.5_总体审计与产品收口检查.md`。

## 阶段 10：上线准备 / 移动端交互补齐 / 部署配置 / 最终产品收口

- [x] 移动端消息操作菜单可用（⋯ 按钮 + popover 菜单）。
- [x] 移动端版本切换可用（箭头常驻显示，不依赖 hover）。
- [x] 移动端单条消息"提炼记忆"可用。
- [x] 桌面端 hover 操作保持正常。
- [x] hosted_encrypted 模式 Settings 页面和聊天页有非流式提示。
- [x] 路由级 code splitting 完成（React.lazy + Suspense，9 个页面拆分）。
- [x] 主入口 JS 体积从 620 kB 降至 399 kB，chunk warning 消除。
- [x] 生产前 console 输出已审查（4 处 info 移除、1 处 debug 加 DEV 守卫、warn/error 保留）。
- [x] 无敏感信息（API Key / encryption secret / service role）泄露到日志。
- [x] .env.example 包含全部必需变量和说明。
- [x] CORS 支持 localhost 开发和生产域名扩展。
- [x] 部署上线指南已创建（docs/roleplay-tavern/部署上线指南.md）。
- [x] 用户使用指南已创建（docs/roleplay-tavern/用户使用指南.md）。
- [x] README.md 已更新（项目介绍 + 快速启动 + 文档索引）。
- [x] 验收清单已同步（阶段 4/5 遗留项修正，阶段 7-10 状态对齐）。
- [x] npm run build 通过，无 chunk warning。
- [x] npx tsc -b 通过，0 错误。
- [x] git status 干净。
- [ ] 全链路回归测试通过（需人工确认）。
- [ ] 移动端真机测试通过（需人工确认）。
- [ ] Supabase Edge Functions 部署验证（需实际部署环境）。
- [x] 阶段 10 执行记录已创建。
